
  # Design BeaconGuard UI (Copy)

  This is a code bundle for Design BeaconGuard UI (Copy). The original project is available at https://www.figma.com/design/fKg1XlLN1TQNVNAdcaCdid/Design-BeaconGuard-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  